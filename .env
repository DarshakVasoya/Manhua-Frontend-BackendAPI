# MongoDB connection string (update credentials and host as needed)
MONGO_URI=mongodb://darshak:DarshakVasoya1310%40@165.232.60.4:27017/admin?authSource=admin

# Redis connection settings for caching
REDIS_HOST=localhost           #user redis for docker, Hostname of Redis service in Docker Compose
REDIS_PORT=6379            # Default Redis port
REDIS_DB=0                 # Redis database index

# Allowed origins for CORS (comma-separated)
CORS_ORIGINS=http://157.230.169.8:3000 
# ip address of droplet

# Minimum size (in bytes) for GZIP compression
GZIP_MIN_SIZE=500

# Base URL for your backend API
BASE_URL=https://manhwagalaxy.org

# Directory paths (used internally)
BACKEND_DIR=/root/frontend/Manhua-Frontend-BackendAPI
FRONTEND_DIR=/root/frontend/Manhua-Frontend